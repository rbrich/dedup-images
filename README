ImageDups
=========

Finds duplicate images using pHash library.

There are three algorithms for image comparison:
 * dct - Standard, works well
 * mh - Slower
 * radial - Faster

For usage info, see:
    imagedups --help


Installation
------------

Build:
    ./setup.py build

Build requires:
    python3-dev
    cython3
    libphash-dev

Install:
    sudo ./setup.py install


Programming Documentation
-------------------------

Build:
    cd doc
    make html

Build requires:
    python3-sphinx

Read in default browser:
    make read
